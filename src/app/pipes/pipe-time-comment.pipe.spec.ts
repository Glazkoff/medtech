import { PipeTimeCommentPipe } from './pipe-time-comment.pipe';

describe('PipeTimeCommentPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeTimeCommentPipe();
    expect(pipe).toBeTruthy();
  });
});
